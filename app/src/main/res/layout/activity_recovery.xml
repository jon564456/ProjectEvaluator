<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    tools:context=".ui.crearcuenta.CreateAccountActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:src="@drawable/logo" />

            <TextView
                style="@style/tag.blue"
                android:text="Recuperar contraseña"
                android:textSize="20sp"
                android:textStyle="bold" />

            <LinearLayout
                style="@style/fondoEditText"
                android:gravity="center">


                <EditText
                    android:id="@+id/rec_txt_correo"
                    style="@style/fondoEditText.editext"
                    android:drawableStart="@drawable/mail"
                    android:hint="Correo electrónico"
                    android:inputType="textEmailAddress" />
            </LinearLayout>

            <Button
                android:id="@+id/rec_btn_continuar"
                style="@style/botonprimary"
                android:text="Continuar" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>